# Custom fonts

The app uses **Manrope** only (Regular, Medium, Bold, ExtraBold) from `@expo-google-fonts/manrope`, loaded in `app/_layout.tsx`. All text — body, headings, and numerical values — uses Manrope via `Fonts` in `constants/theme.ts`.
